{
  "buildCommand": "nx build client --prod",
  "builds": [
    {
      "src": "dist/apps/client/*",
      "use": "@vercel/static"
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "/index.html"
    }
  ]
}
